<% provide(:title, 'Statistic') %>
<h1>Statistic</h1>
<p>This page is a sample for the data visualization in Hyve.me and shows the number of data items collected over time.</p>
<h3>Graph</h3>
<%= high_chart("my_bar_div", @graph) %>
    <h3>Data</h3>
      <table class="table table-striped table-bordered table-condensed">
        <thead><tr>
          <th>week</th>
          <th># Data Items</th>
        </tr></thead>
        <% @datavals.each do |d| %>
          <tr>
            <td><%= Date.strptime(d[0]).strftime("%F") %></td>
            <td><%= d[1] %></td>
          </tr>
        <% end unless @datavals.nil? %>
      </table>

