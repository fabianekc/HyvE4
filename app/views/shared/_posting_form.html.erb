<%= form_for(@posting) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new posting...", :rows => "2" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary pull-right" %>
<% end %>
